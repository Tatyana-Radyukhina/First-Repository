package Special;

import java.util.Scanner;

 /*public class Book_store {

     public static void main(String[] args) {
         Scanner scanner = new Scanner(System.in);
         String title = scanner.nextLine();
         boolean isCorrectTitle = true;
         isCorrectTitle = checkTitle(title);
         while (isCorrectTitle){
             System.out.println("Книга есть в картотеке");
             break;
         }
     }

     public static boolean checkTitle(String title) {
         String[] cartoteka = {"airport", "hotel", "birds"};
         for (int i = 0; i < cartoteka.length; i++) {
             cartoteka[i].equals(title);
         }

         return false;
     }
 } */
/*
public class Book_store {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        String title = scanner.nextLine();
        boolean isCheckTitle = true;
        isCheckTitle = checkTitle(title);
        checkTitle(title);


    }

    private static boolean checkTitle(String title) {
        String[] cartoteka = {"airport", "hotel", "birds"};
        for (int i = 0; i < cartoteka.length; i++) {
            if (cartoteka[i].equals(title)) {
                System.out.println("Книга есть в картотеке");
                return true;
            }
        }
    }
}

*/